import React from "react";
import { styled } from "@mui/system";
import { Button } from "@mui/material";

export const CButtonSpan = styled(
  Button,
  {}
)({
  color: "#f5f5f5",
  borderRadius: "0px",
});
export const CButtonPrimary = styled(
  Button,
  {}
)({
  backgroundColor: "#6e6f70",
  color: "#d5dde1",
  borderRadius: "0px",
});
