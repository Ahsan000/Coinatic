import { Typography } from "@mui/material";
import React from "react";

const GreenValue = ({ value }) => {
  return (
    <div>
      <Typography variant="subtitle1">{value}</Typography>
    </div>
  );
};

export default GreenValue;
