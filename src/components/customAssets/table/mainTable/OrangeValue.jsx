import { Typography } from "@mui/material";
import React from "react";

const OrangeValue = ({ value }) => {
  return (
    <div>
      <Typography variant="span">{value}</Typography>
    </div>
  );
};

export default OrangeValue;
